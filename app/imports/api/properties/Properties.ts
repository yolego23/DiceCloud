import { ActionProperty, ComputedActionProperty, ComputedOnlyActionProperty } from './Actions';

export const Properties = {
  action: ActionProperty,
}

export const ComputedOnlyProperties = {
  action: ComputedOnlyActionProperty,
}

export const ComputedProperties = {
  action: ComputedActionProperty,
}
